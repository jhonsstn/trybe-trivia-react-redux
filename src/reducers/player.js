const INITIAL_STATE = {
  name: '',
  assertions: 0,
  score: 0,
  gravatarEmail: '',
};

function player(state = INITIAL_STATE, actions) {
  switch (actions.type) {
  default:
    return state;
  }
}

export default player;
